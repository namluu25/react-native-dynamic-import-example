import { View } from "react-native";

export default function Circle() {
  return (
    <View
      style={{
        width: 100,
        height: 100,
        borderRadius: 50,
        backgroundColor: "red",
      }}
    />
  );
}
